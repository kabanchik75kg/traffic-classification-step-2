# ВКР — окружение для этапа 2 (sequence-представление)
# Python 3.11 или 3.12. Установка: pip install -r requirements.txt

# --- чтение и обработка данных ---
numpy>=1.26
pandas>=2.1
pyarrow>=14.0          # чтение parquet (им же писался датасет)
duckdb>=0.10           # работа с большими parquet без полной загрузки в RAM

# --- классические модели (baseline этапа 1, для сопоставимости) ---
scikit-learn>=1.3
xgboost>=2.0

# --- sequence-модели (1D-CNN / BiLSTM) ---
torch>=2.2             # CPU-сборка ставится по умолчанию; про GPU см. примечание ниже

# --- графики для отчёта и прогресс-бары ---
matplotlib>=3.8
tqdm>=4.66

# ------------------------------------------------------------------
# ОПЦИОНАЛЬНО (ставить позже, по мере необходимости — не сейчас):
# lightgbm>=4.0        # альтернативный табличный baseline
# nfstream>=6.6        # только если будем заново собирать CIC-IDS2017 из pcap (этап 2.6); требует системный libpcap
# torch-geometric      # графовый этап 3 (опционально)